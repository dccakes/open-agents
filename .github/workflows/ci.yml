name: CI

on:
  pull_request:
    branches: ["*"]

jobs:
  lint-and-typecheck:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint and format check
        run: bun run check

      - name: Typecheck
        run: bun run typecheck

      # This job duplicates the steps in the root `ci` script rather than
      # invoking it, so anything added there must be added here too.
      - name: Env boundary
        run: bun run lint:env-boundary

      - name: Test
        run: bun run test:isolated

      - name: DB Check
        run: bun run --cwd apps/web db:check
